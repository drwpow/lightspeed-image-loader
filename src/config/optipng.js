module.exports = {
  bitDepthReduction: true, // Apply bit depth reduction.
  colorTypeReduction: true, // Apply color type reduction.
  optimizationLevel: 3, // Select a number between 0 (fast, weighty) to 7 (slow, lightweight)
  palletteReduction: true, // Apply palette reduction.
};
