{
  "type": "object",
  "properties": {
    "f": {
      "description":
        "Shortcut for `format`. Specify `jpg`, `webp`, or `png` to convert formats. By default, no conversion will take place.",
      "type": "string"
    },
    "format": {
      "description":
        "Specify `jpg`, `webp`, or `png` to convert formats. By default, no conversion will take place.",
      "type": "string"
    },
    "h": {
      "description":
        "Shortcut for `height`. Specify a number to downsize the image width to (in pixels). Width will scale automatically. Won’t upscale image. If `height` and `width` are specified, width takes priority.",
      "type": ["number", "string"]
    },
    "height": {
      "description":
        "Specify a number to downsize the image width to (in pixels). Width will scale automatically. Won’t upscale image. If `height` and `width` are specified, width takes priority.",
      "type": ["number", "string"]
    },
    "inline": {
      "description":
        "Set to `?inline` or `?inline=true` to return SVG markup for SVG files, or base64 encoding for other files.",
      "type": ["string", "boolean"]
    },
    "interpolation": {
      "description":
        "When scaling, specify `'nearest'` for nearest-neighbor (pixel art), `'cubic'` for cubic interpolation, or `'lanczos2'` or `'lanczos3'` for [Lanczos][lanczos] with `a=2` or `a=3`. `'cubic'` is this loader’s default (because it’s what most are used to), as opposed to`'lanczos3'` which is sharp’s default (present for other loaders)",
      "type": "string"
    },
    "q": {
      "description":
        "Shortcut for `quality`. Specify a number, `1`–`100`, to reduce the image’s quality and filesize.",
      "type": ["number", "string"]
    },
    "quality": {
      "description":
        "Specify a number, `1`–`100`, to reduce the image’s quality and filesize.",
      "type": ["number", "string"]
    },
    "skip": {
      "description":
        "Set to `?skip` or `?skip=true` to bypass resizing & optimization entirely. Useful for problem images.",
      "type": ["string", "boolean"]
    },
    "w": {
      "description":
        "Shortcut for `width`. Specify a number to downsize the image width to (in pixels). Height will scale automatically. Won’t upscale image. Ignored for SVG.",
      "type": ["number", "string"]
    },
    "width": {
      "description":
        "Specify a number to downsize the image width to (in pixels). Height will scale automatically. Won’t upscale image. Ignored for SVG.",
      "type": ["number", "string"]
    }
  },
  "additionalProperties": false
}
