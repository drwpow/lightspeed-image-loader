{
  "type": "object",
  "properties": {
    "emitFile": {
      "description":
        "Set to `false` to skip processing file (for server-side rendering, e.g.).",
      "type": "boolean"
    },
    "gif": {
      "description": "Specify GIFSicle options.",
      "type": "object"
    },
    "jpg": {
      "description": "Specify JpegOptim options. ",
      "type": "object"
    },
    "jpeg": {
      "description": "Alias of `jpg`. Specify JpegOptim options.",
      "type": "object"
    },
    "outputPath": {
      "description": "Override webpack’s default output path for these images.",
      "type": "string"
    },
    "png": {
      "description": "Specify OptiPNG and PNGquant options together. ",
      "type": "object"
    },
    "q": {
      "description":
        "Alias of `quality`. Specify a number, `1`–`100`, to set the fallback quality when none is specified.",
      "type": ["number", "string"]
    },
    "quality": {
      "description":
        "Specify a number, `1`–`100`, to set the fallback quality when none is specified.",
      "type": ["number", "string"]
    },
    "svg": {
      "description": "Alias of `svgo`. Override SVGO default settings.",
      "type": "object"
    },
    "svgo": {
      "description": "Override SVGO default settings.",
      "type": "object"
    }
  },
  "additionalProperties": false
}
