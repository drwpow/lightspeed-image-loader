{
  "type": "object",
  "properties": {
    "emitFile": ["boolean"],
    "gif": {
      "description": "Specify GIF-specific options.",
      "type": "object"
    },
    "jpg": {
      "description": "Specify JPG-specific options.",
      "type": "object"
    },
    "jpeg": {
      "description": "Alias of `jpg`. Specify JPG-specific options.",
      "type": "object"
    },
    "outputPath": ["string"],
    "png": {
      "description": "Specify PNG-specific options.",
      "type": "object"
    },
    "q": {
      "description":
        "Alias of `quality`. Specify a number, `1`–`100`, to reduce the image’s quality and filesize. `100` will skip optimization entirely.",
      "type": ["number", "string"]
    },
    "quality": {
      "description":
        "Specify a number, `1`–`100`, to reduce the image’s quality and filesize. `100` will skip optimization entirely.",
      "type": ["number", "string"]
    },
    "svg": {
      "description": "Alias of `svgo`. Override SVGO default settings.",
      "type": "object"
    },
    "svgo": {
      "description": "Override SVGO default settings.",
      "type": "object"
    }
  },
  "additionalProperties": false
}
